/**
 * The Settings window. docs/06 Phase 11.
 *
 * A second OS window rather than a sheet — see `SettingsWindow`. The sections it shows still
 * live with the features they configure; what is here is the window, the pane list, and the
 * four panes that have no feature of their own to live in.
 */

export { SettingsWindow } from './SettingsWindow'
export { PANES, paneFrom, type PaneDescriptor } from './panes'
